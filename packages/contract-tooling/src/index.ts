/**
 * Public surface of `@marcos-corp/contract-tooling`.
 *
 * Placeholder: the house ruleset wrapper (`lintSpec`), the breaking-change
 * diff (`diffSpecs`), the published-baseline resolver, the OpenAPI emitter,
 * the `@marcos-corp/db` dependency check, the exact-pin check and the gate
 * runner that composes them land in later tasks and are re-exported here.
 */

export {};
