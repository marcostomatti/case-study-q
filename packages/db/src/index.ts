/**
 * Public surface of `@marcos-corp/db`.
 *
 * Placeholder: the Drizzle tables (companies, cards, spend limits,
 * transactions, invoices, api_usage) and their migrations land in later
 * tasks and are re-exported from here. Nothing under `packages/contracts-*`
 * may ever import this module — a CI gate enforces that.
 */

export {};
